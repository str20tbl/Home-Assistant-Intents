language: sk
responses:
  intents:
    HassVacuumCleanArea:
      default: "Idem povysávať {{ slots.area }}"
      here: "{{ slots.area }} bude povysávaná"
      area: "Budem vysávať {{ slots.area }}"

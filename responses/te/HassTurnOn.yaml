language: te
responses:
  intents:
    HassTurnOn:
      default: Turned on {{ slots.name }}
      lights_area: Turned on lights in {{ slots.area }}
      fans_area: fans in {{ slots.area }} on చేయబడింది
      cover: Opened {{ slots.name }}
      cover_area: Opened {{ slots.area }}

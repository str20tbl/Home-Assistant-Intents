language: zh-CN
intents:
  HassIncreaseTimer:
    data:
      - sentences:
          - "[给]<timer>(增加|延长|加)<timer_duration>"
          - "[给]<timer>再加<timer_duration>"
          - "[给]<timer>再<timer_duration>"
      - sentences:
          - "[给]<area><timer>(增加|延长|加)<timer_duration>"
          - "[给]<area><timer>再加<timer_duration>"
      - sentences:
          - "[给]{timer_name:name}<timer>(增加|延长|加)<timer_duration>"
          - "[给]{timer_name:name}<timer>再加<timer_duration>"

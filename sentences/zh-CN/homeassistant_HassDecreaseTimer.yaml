language: zh-CN
intents:
  HassDecreaseTimer:
    data:
      - sentences:
          - "[给]<timer>(减少|缩短|减)<timer_duration>"
          - "[给]<timer>再减<timer_duration>"
          - "[给]<timer>再少<timer_duration>"
      - sentences:
          - "[给]<area><timer>(减少|缩短|减)<timer_duration>"
          - "[给]<area><timer>再减<timer_duration>"
      - sentences:
          - "[给]{timer_name:name}<timer>(减少|缩短|减)<timer_duration>"
          - "[给]{timer_name:name}<timer>再减<timer_duration>"

---
# HassVacuumReturnToBase - name_only
# example: return rover to base
# slots: {name}

language: "en"
data:
  - sentences:
      - "return [<the>] {name} [to base]"
    example: "return rover to base"
    name_domains:
      - "vacuum"
    response: "default"

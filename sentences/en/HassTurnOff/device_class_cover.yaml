---
# HassTurnOff - device_class_cover
# example: close the windows in here
# slots:
# - {domain}
# - {device_class}
# context_area: true

language: "en"
data:
  - sentences:
      - "<close> [<the>] {cover_classes:device_class} [<here>]"
    example: "close the curtains in here"
    inferred_domain: "cover"
    response: "cover_device_class"

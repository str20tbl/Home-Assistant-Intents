language: fr
intents:
  HassListCompleteItem:
    data:
      # name and item
      - sentences:
          - "<coche> <item> (de|dans|sur) [<le>]{name}"
          - "<coche> <item> (de|dans|sur) [la] liste [<de>] {name}"
          - "<coche> (de|dans|sur) [<le>]{name} <item>"
        requires_context:
          domain: todo
        response: item_completed
        expansion_rules:
          item: "{shopping_list_item:item}"

---
# HassVacuumReturnToBase - name_only
# example: return rover to base

language: "en"
entities:
  - name: "Rover"
    domain: "vacuum"

tests:
  - sentences:
      - "return rover to base"
    slots:
      name: "Rover"
    response: "Returning"

---
# HassGetCurrentTime - default
# example: what time is it

language: "en"
tests:
  - sentences:
      - "what's the time"
      - "what time is it"
      - "what time is it right now"
      - "tell me the time"
    response: "1:02 AM"

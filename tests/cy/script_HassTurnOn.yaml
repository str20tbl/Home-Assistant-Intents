language: cy
tests:
  # VERIFY (machine-drafted): rhedeg sgript wrth enw / run a script by name
  - sentences:
      - "rhedeg modd cudd"
      - "actifadu modd cudd"
      - "modd cudd ymlaen"
      - "dechrau modd cudd sgript"
    intent:
      name: HassTurnOn
      slots:
        domain: script
        name: Modd Cudd
    response: "Wedi cychwyn"

language: cy
tests:
  # VERIFY (machine-drafted): diffodd dyfais wrth enw / turn off a device by name
  - sentences:
      - "troi ceiling fan i ffwrdd"
      - "diffodd ceiling fan"
    intent:
      name: HassTurnOff
      slots:
        name: Ceiling Fan
    response: "Wedi diffodd y ffan"
  - sentences:
      - "troi kitchen switch i ffwrdd"
      - "diffodd kitchen switch"
    intent:
      name: HassTurnOff
      slots:
        name: Kitchen Switch
    response: "Wedi diffodd y switsh"
